<div class="row">
  <div class="span6 offset3">
    <h1>Articles</h1>
    <ul>
      <% @articles.each do |article|%>
      <li> <%= link_to article.title.titleize, article %> | Posted <%= time_ago_in_words(article.created_at) %> </li>
      <% end %>
    </ul>
  </div>
</div>